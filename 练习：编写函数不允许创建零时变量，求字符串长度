#include <stdio.h>
#include <string.h>
#include <stdlib.h>
 int my_lenth(char* x)
{
	 
	 if (*x != 0)
	 {
		 return 1 + my_lenth(x+1);
	 }
	 else
		 return 0;
}

int main()
{
	char arry[] = "bit";
	int L = 0;
	L=my_lenth(arry);
	printf("L=%d\n", L);
	return 0;
}
