#include <stdio.h>
#include <string.h>
#include <stdlib.h>
 void print(int x)
{
	if (x > 9)
	{		
		print(x/10);
	}
	printf(" %d", x%10);
}

int main()
{
	int num = 0;
	scanf_s("%d", &num);
	print(num);
	return 0;
}
